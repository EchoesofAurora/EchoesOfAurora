Defines data models (schemas) for database interactions.
Example models:
User.js: Schema for user data (name, email, password).
Story.js: Schema for aurora stories.
Event.js: Schema for aurora events.
